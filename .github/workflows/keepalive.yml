name: Keep n8n Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping n8n
        run: |
          curl -f https://your-app-name.onrender.com/healthz
          echo "n8n is alive ✅"
