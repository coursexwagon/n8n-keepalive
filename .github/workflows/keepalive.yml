name: Keep n8n Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping n8n
        run: |
          curl -f https://n8n-automations-hfzu.onrender.com
          echo "n8n is alive ✅"
